<h1>User info</h1>
<%= attachment_image_tag @user, :image, :fill, 60, 60, fallback: "no_image.jpg" %>
<table>
  <tr>
    <td>name</td>
    <td><%= @user.name %></td>
  </tr>
  <tr>
    <td>introduction</td>
    <td><%= @user.introduction %></td>
  </tr>
</table>
<%= link_to "btn_edituser", edit_user_path(@user.id) %>

<h1>New book</h1>
<%= form_with model: @book, url: books_path, method: :post, local: true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :opinion %>
  <%= f.submit %>
<% end %>

<h1>Books</h1>
<table>
  <thead>
    <tr>
      <th>userimage</th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
  </thead>
  <% @books.each do |book| %>
   <tr>
     <td>"userimage"</td>
     <td><%= link_to book.title, book_path(book.id) %></td>
     <td><%= book.opinion %></td>
   </tr>
  <% end %>
</table>
